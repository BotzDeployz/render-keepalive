name: 🚀 Keep Render Alive ULTRA

on:
  schedule:
    - cron: '*/4 * * * *'  # Каждые 4 минуты!
  
  workflow_dispatch:
  push:
    branches: [main]

jobs:
  ping:
    runs-on: ubuntu-latest
    timeout-minutes: 1
    
    steps:
      - name: 🔁 Ping Both Services
        run: |
          echo "🕒 Ping started at: $(date -u '+%H:%M:%S UTC')"
          
          # Простой пинг без сложных проверок
          curl -s -m 25 "https://spotter-backend-zxsj.onrender.com" > /dev/null && echo "✅ Backend pinged"
          curl -s -m 25 "https://spotter-frontend-1.onrender.com" > /dev/null && echo "✅ Frontend pinged"
          
          echo "✅ Ping completed"
